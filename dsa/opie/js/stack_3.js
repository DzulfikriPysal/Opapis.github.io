function stack_b(arrays){
		
		arrays_length = arrays.length;
		
		if(arrays_length > 3){
			//array more that 3
			var strategy_type = "OK";

		}
		else{
			// array less than 3
			var strategy_type = "Undefined strategy";
		}
		
		
		return strategy_type;
			
		
}